```{=html}
<div class="list">
  <% for (const item of items) { %>
  <div <%= metadataAttrs(item) %> class="card" style="margin: 1em 0">
    <div class="card-body" style="display: flex;">
      <div style="flex: 0 0 35%; border-right: solid 1px rgba(0,0,0,.125)">
        <h3 class="listing-title" style="margin-top: 0;"><%= item.title %></h3>
        <p class="subtitle lead"><%= item.location %></p>

        <% if (item.website) { %>
        <a href="<%= item.website %>" class="btn btn-primary btn-sm">Website</a>
        <% } %>
        <% if (item.report) { %>
        <a href="<%= item.report %>" class="btn btn-primary btn-sm">Report</a>
        <% } %>
        <% if (item.proceedings) { %>
        <a href="<%= item.proceedings %>" class="btn btn-primary btn-sm">Proceedings</a>
        <% } %>
      </div>

      <% if (item.keynotes) { %>
        <% for (const keynote of item.keynotes) { %>
        <div class="card border-light" style="flex: 0 0 30%; min-width:200px; max-width:500px; margin: 0 0.5em;">
          <div class="card-body">
            <img src="photos/<%= keynote.photo %>" alt="<%= keynote.speaker %>" style="width: 100%; object-fit: cover; height: auto !important; aspect-ratio: 1; margin-bottom: 0.5em">
            <h6 class="card-title" style="margin-top: 0;"><%= keynote.talk %></h6>
            <h6 class="card-subtitle mb-2 text-muted"><a href="<%= keynote.website %>"><%= keynote.speaker %></a></h6>
          </div>
        </div>
        <% } %>
      <% } %>
      <% if (!item.keynotes) { %>
        <p style="flex: 0 0 60%; align-self: center; text-align: center;">
          <i>Keynote speakers to be announced</i>
        </p>
      <% } %>
    </div>
  </div>
  <% } %>
</div>
```
